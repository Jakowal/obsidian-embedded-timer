import { TimerModel } from "src/models/timer.model";

export const timerIsFinished = (timer: TimerModel, onComplete?: () => void): boolean => {
    const currentTime = new Date();

    

    return false;
}